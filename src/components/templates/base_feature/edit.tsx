import React from "react";
import EditBaseFeature from "../../UI/base_feature/EditBaseFeature";

const templates = {
    content: {
        body: <EditBaseFeature/>
    }
}
export const edit = [templates.content]