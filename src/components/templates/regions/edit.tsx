import React from "react";
import EditRegionForm from "../../UI/regions/EditRegionForm";

const templates = {
    content: {
        body: <EditRegionForm/>
    }
}
export const edit = [templates.content]