import React from "react";
import EditProductType from "../../UI/product_types/EditProductType";

const templates = {
    content: {
        body: <EditProductType/>
    }
}
export const edit = [templates.content]