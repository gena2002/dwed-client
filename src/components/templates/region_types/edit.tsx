import React from "react";
import EditRegionType from "../../UI/region_types/EditRegionType";

const templates = {
    content: {
        body: <EditRegionType/>
    }
}
export const edit = [templates.content]